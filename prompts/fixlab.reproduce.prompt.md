---
mode: agent
description: Reproduce the diagnosed defect with the smallest reliable check.
---

Use the repository profile to select the narrowest existing test or safe
runtime journey that demonstrates the diagnosed behavior. Add focused
regression coverage only when existing coverage cannot prove the requirement.
Record the failing result before implementation. If reproduction is unsafe or
blocked, stop and report the exact missing prerequisite.
